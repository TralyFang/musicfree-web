name = "musicfree-cors-proxy"
main = "index.ts"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "production"

# 自定义域名路由（绕过 workers.dev 被墙问题）
[[routes]]
pattern = "proxy.dailywhy.top/*"
zone_name = "dailywhy.top"
